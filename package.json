{
  "name": "tft",
  "type": "module",
  "private": true,
  "scripts": {
    "zig:build": "zig build",
    "calc": "bun run zig:build && bun src/calc.ts",
    "screenshot": "bun scripts/screenshot.ts",
    "format": "oxfmt --no-error-on-unmatched-pattern"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.65.0"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
