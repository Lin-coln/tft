{
  "name": "tft",
  "type": "module",
  "private": true,
  "scripts": {
    "build:zig": "zig build",
    "calc": "bun run build:zig && bun src/calc.ts",
    "format": "oxfmt --no-error-on-unmatched-pattern"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.65.0"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
